export function TeacherPrepShelfEmptyState() {
  return (
    <div
      style={{
        marginTop: 12,
        border: "1px dashed rgba(0,0,0,0.18)",
        borderRadius: 14,
        padding: 18,
        color: "#78716c",
        background: "rgba(255,255,255,0.55)",
      }}
    >
      No prep books yet.
    </div>
  );
}